{% extends 'partials/base.html.twig' %}

{% block content %}
    <div class="half-splash">
        {% include 'partials/media.html.twig' %}
    </div>
    <div class="container">
      <article class="post-content">
        <h1 class="post-title">{{ page.title }}</h1>
        <div class="post-content">{{ page.content }}</div>
      </article>
    </div>
    <!-- <div class="container">
      <div class="project-list row">
        {% for post in site.categories[page.slug] %}
          <div class="col-sm-2">
            <div class="project-desc">
              <div>
                <h2>
                  {{ post.title }}
                </h2>
                <a class="post-link" href="{{ post.url }}"><img src="{{site.baseurl}}/assets/images/00homepage_circlethumbs/{% if post.thumbnail %}{{post.thumbnail}}{% else %}cw4.svg{% endif %}" /></a>
                <div class="description">{{post.description}}</div>
              </div>
            </div>
          </div>
        {% endfor %}
      </div>
    </div> -->
{% endblock %}